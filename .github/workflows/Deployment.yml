name: Deployment
env:
  NAME: 'Snyk Demo'

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  runs-on: ubuntu-latest
  deployment:
    environment: github-pages
    steps:
    - name: Print name
      run: echo "Hello $NAME"